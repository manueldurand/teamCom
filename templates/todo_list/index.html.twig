{% extends 'base.html.twig' %}

{% block title %}Todolist{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Todo List : </h1>
    <h2>Bonjour {{ nom }}</h2>

<table border>
<thead>
<tr>
<td>n°</td><td>date</td><td>auteur</td><td>description</td><td>comment</td>
<td>modif/</td><td>par</td><td>date</td>
</tr>
</thead>
<tbody>
    {% for todo in todo_list%}
<tr>
<td>{{ todo.id }}</td>
<td>{{ todo.created|date("d/m/Y") }}</td>
<td>{{ todo.auteur }}</td>
<td>{{ todo.description }}</td>
<td>{{ todo.comment }}</td>
<td>{{ todo.reponse }}</td>
<td>{{ todo.auteurReponse }}</td>
<td>{{ todo.dateReponse|date("d/m/Y")}}</td>

{% if is_granted('ROLE_USER') %}
<td><form action="todolist/delete/{{todo.id}}" method="post">
    {# the argument of csrf_token() is an arbitrary string used to generate the token #}
    <input type="hidden" name="token" value="{{ csrf_token('delete-item') }}"/>

    <button type="submit">Supprimer</button>
</form></td>
<td><a href="{{path('update_todo', {id: todo.id})}}"><button>modifier</button></a></td>

{% endif %}
</tr>
{% endfor %}




</div>
{#<a href="/todolist/delete/{{todo.id}}" ><button>SUPPRIMER</button></a>
{{ url('/todolist/delete', { id: todo.id }) }}#}
{% endblock %}
